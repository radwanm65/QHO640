import React from 'react';

// Create a context with a default value "No name"
const NameContext = React.createContext("No name");

export default NameContext;  // Export the context to be used in other components
